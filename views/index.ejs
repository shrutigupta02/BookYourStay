<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book Your Stay</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            padding: 20px;
        }
        h1 {
            margin-bottom: 20px;
        }
        .hotels-container {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
        }
        .hotel-card {
            flex: 1 1 calc(33.333% - 20px); /* Adjusts the card width to one-third minus the gap */
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border: none;
        }
        .hotel-card img {
            width: 100%;
            height: 200px; /* Set a fixed height */
            object-fit: cover; /* Ensures the image covers the area and maintains aspect ratio */
        }
        .btn-new {
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1 class="text-center">HOTELS</h1>
        <form method="get" action="/hotels/new">
            <button class="btn btn-primary btn-new">NEW</button>
        </form>
        <div class="hotels-container">
            <% for(hotel of hotels){ %>
            <div class="hotel-card card">
                <img src="<%= hotel.image.url %>" class="card-img-top" alt="<%= hotel.title %>">
                <div class="card-body">
                    <h2 class="card-title"><a href="/hotels/<%= hotel.id %>"><%= hotel.title %></a></h2>
                    <p class="card-text"><%= hotel.description %></p>
                </div>
            </div>
            <% } %>
        </div>
    </div>
</body>
</html>
